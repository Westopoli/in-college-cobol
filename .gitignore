# Build + test-run artifacts (regenerated by RunTests.cob — don't commit)
# InCollege-Input.txt / InCollege-Output.txt are NOT ignored: the PDF
# deliverables (#3/#4) require a committed root-level sample pair, so
# RunTests.cob overwriting them during a local run is expected — just
# don't commit over the deliverable version without meaning to.
incollege
runtests
*.o
*.so
accounts.txt
profiles.txt
results/
*.tmp
.got.tmp
testlist.tmp
